// Booking form — room picker, date/time, reason, submit (Saksham)
// Submit → POST /api/bookings/request

const BookingsPage = () => (
  <div className="flex flex-col gap-4 p-4">
    <h1 className="text-xl font-semibold">Book a Room</h1>
    {/* TODO: BookingForm — room, start/end time, reason, submit */}
    <p className="text-muted-foreground text-sm">Booking form — wire to /api/bookings/request</p>
  </div>
);

export default BookingsPage;
