// Admin bookings tab — pending table, approve/reject (Saksham)

const AdminBookingsPage = () => (
  <div className="flex flex-col gap-4 p-4">
    <h1 className="text-xl font-semibold">Pending Bookings</h1>
    {/* TODO: BookingsTable — approve/reject buttons, wire to /api/admin/approve, reject */}
    <p className="text-muted-foreground text-sm">BookingsTable — wire to admin API</p>
  </div>
);

export default AdminBookingsPage;
