# ─── Database (Neon — use the DIRECT endpoint, not pooler) ────
DATABASE_URL=postgresql://user:pass@ep-xxx.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

# ─── Internal Secret (must match root .env / Next.js) ────────
INTERNAL_SECRET=same-long-random-string-as-root-env

# ─── LLM (Groq) ──────────────────────────────────────────────
GROQ_API_KEY=

# ─── Google Drive (for document sync) ────────────────────────
GOOGLE_DRIVE_FOLDER_ID=
GOOGLE_SERVICE_ACCOUNT_JSON=

# ─── CORS (comma-separated origins allowed) ──────────────────
CORS_ORIGINS=http://localhost:3000
